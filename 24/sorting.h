#include <stdlib.h>
#include <stdio.h>

// SORTING FUNCTIONS

extern void swap(int *a, int *b);

extern size_t partition(int arr[], size_t low, size_t high);

extern void quicksort(int arr[], size_t low, size_t high);